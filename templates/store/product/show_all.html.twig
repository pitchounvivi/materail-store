{% extends 'store/product/_base.html.twig' %}

{% block titleContent %}Hello ProductController!{% endblock %}

{% block tableContent %}
    {{ parent() }}

    <h2>Products</h2>
    <table class="table">
        <thead>
        <tr>
            <th>id</th>
            <th>name</th>
            <th>description</th>
            <th>category</th>
        </tr>
        </thead>
        <tbody>
        {% for produit in product %}
            <tr>
                <th>{{produit.id}}</th>
                <td>{{produit.name}}</td>
                <td>{{produit.description}}</td>
                <td>{{produit.category}}</td>
                <td><a href="{{ path("store_product_show",{"id":produit.id})}}">l'afficher</a></td>
            </tr>

        {% endfor %}
        </tbody>
    </table>


{% endblock %}

{% block bodyContent %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/Users/Administrateur/Desktop/symfony/material-store/src/Controller/Store/ProductController.php'|file_link(0) }}">src/Controller/Store/ProductController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/Users/Administrateur/Desktop/symfony/material-store/templates/store/product/show_all.html.twig'|file_link(0) }}">templates/store/product/show_all.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
